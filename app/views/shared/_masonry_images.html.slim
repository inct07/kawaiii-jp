#grid
  - images.each do |image|
    == render "shared/image", image: image, current_user_favorite_images: @current_user_favorite_images

= hidden_field_tag 'authenticity_token', form_authenticity_token
